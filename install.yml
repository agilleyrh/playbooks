---
- hosts: all
  become: true
  tasks:
    - name: Install Vim
      ansible.builtin.yum:
        name: vim
        state: present

    - name: Install Tree
      ansible.builtin.yum:
        name: tree
        state: present
