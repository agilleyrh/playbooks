---
- hosts: all
  become: true
  tasks:
    - name: Check for updates
      ansible.builtin.yum:
        name: "*"
        state: latest
        update_cache: true

    - name: Print list of updates
      ansible.builtin.debug:
        var: yum_updates
